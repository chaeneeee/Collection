package com.choongang;
import javax.swing.*;
import java.util.*;

public class B_getNthElement {
    public Integer getNthElement(ArrayList<Integer> arrayList, int index) {
        // TODO:
//        ArrayList<Integer> getNthElement = new ArrayList<>();
        if (arrayList.size() == 0) {
            return null;
        } else {return arrayList.get(index);



    }
}}